[package]
name = "iem-engine-proto"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "iemmixer engine protocol: stable ids, mix state, commands, replies, events and pipe frames"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
iem-dsp = { path = "../iem-dsp" }
